{
  "name": "tau",
  "version": "0.0.1",
  "description": "Front-facing projects",
  "author": "chartly",
  "license": "MIT",
  "main": "build/www/index.js",
  "devDependencies": {
    "beefy": "^2.1.5",
    "browserify": "^11.0.1",
    "jade": "^1.11.0",
    "mkdirp": "^0.5.1",
    "npm-run-all": "^1.2.6",
    "rimraf": "^2.4.1",
    "stylus": "^0.51.1",
    "tsconfig-glob": "^0.1.3",
    "tsconfig-lint": "^0.1.8",
    "tslint": "^2.3.1-beta",
    "typescript": "^1.5.3",
    "watchify": "^3.3.1"
  },
  "scripts": {
    "build": "npm run build:ts -s && npm run build:styles -s && npm run build:markup -s",
    "build:tslint": "tsconfig-lint .",
    "build:ts": "tsc",
    "build:styles": "stylus assets/styles/app.styl -m -o assets/styles",
    "build:markup": "jade assets/markup/index.jade -o ."
  }
}
